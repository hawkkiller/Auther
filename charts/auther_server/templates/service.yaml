apiVersion: v1
kind: Service
metadata:
  name: {{ include "auther_server.fullname" . }}
  labels:
    {{- include "auther_server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "auther_server.selectorLabels" . | nindent 4 }}
